<p id="notice"><%= notice %></p>

<h2>
  <%= @group.name %>
</h2>
<p><%= "#{@group.location.city}, #{@group.location.state}" if @group.location %></p>
<%= link_to 'Edit', edit_group_path(@group)%>
<%= link_to 'Back', groups_path %>
